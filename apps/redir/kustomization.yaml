apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: redir
resources:
- namespace.yaml
- deployment.yaml
- app-sealed-secret.yaml
- service.yaml
- ingress.yaml
configMapGenerator:
- name: redir
  files:
  - redirects.toml
